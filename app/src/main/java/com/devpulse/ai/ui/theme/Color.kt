package com.devpulse.ai.ui.theme

import androidx.compose.ui.graphics.Color

// Calm Developer Palette (Restrained, Natural, Non-Fatiguing)
val BackgroundDark = Color(0xFF101215)       // Very dark charcoal / near-black
val SurfaceDark = Color(0xFF16191D)          // Slightly lighter charcoal
val SurfaceVariantDark = Color(0xFF1F232A)   // Subtle elevated surface
val BorderDark = Color(0xFF282E37)           // Subtle, non-glowing border
val BorderSubtle = Color(0xFF1E2229)         // Very gentle divider

// Typography Colors
val TextPrimaryDark = Color(0xFFEDEFEF)      // Warm off-white
val TextSecondaryDark = Color(0xFF94A3B8)    // Muted gray-blue
val TextMuted = Color(0xFF64748B)            // Low-emphasis supporting text

// Semantic Accents
val SageGreen = Color(0xFF7CA982)            // Growth / 1% Better / Nature / Positive Recovery
val MutedLavender = Color(0xFF9D95B8)        // Primary interactive accent
val MutedBlueGray = Color(0xFF64748B)        // Tertiary supporting accent
val MutedAmber = Color(0xFFD69E66)           // Attention / Reminder / Pause state
val MutedRed = Color(0xFFC86D62)             // Destructive / Error states only

// Aliases mapped to semantic accents (replacing neon cyan & neon purple)
val Primary = MutedLavender                  // Interactive accent
val Secondary = SageGreen                    // Growth & 1% Better accent
val Tertiary = MutedBlueGray

// Light Palette Colors (For complete Material 3 definitions)
val PrimaryLight = Color(0xFF5D5575)
val SecondaryLight = Color(0xFF4C7552)
val BackgroundLight = Color(0xFFF7F8F9)
val SurfaceLight = Color(0xFFFFFFFF)
val TextPrimaryLight = Color(0xFF191C1E)
val TextSecondaryLight = Color(0xFF535F69)

